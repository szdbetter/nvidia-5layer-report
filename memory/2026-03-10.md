
## OpenViking → Gemini 迁移完成 (18:51 GMT+8)

**根本原因**: openclaw.json 插件配置多包一层 `config: {}`，导致 `api.pluginConfig.mode = undefined` → 默认 local 模式 → waitForHealth 超时报 "OpenViking health check timeout"。

**修复**:
1. `ov.conf`: embedding 改为 `models/gemini-embedding-001`（3072维，Google OpenAI-compat endpoint）
2. `ov.conf`: VLM 改为 `gemini-2.0-flash-lite`
3. `openclaw.json`: memory-openviking 插件配置 mode/baseUrl 从 `entry.config.mode` 提到 entry 顶层
4. systemd 服务 `/etc/systemd/system/openviking.service` 创建，注入 GOOGLE_API_KEY

**验证**: memory_store ✅ memory_recall ✅ curl /health ✅

**项目文档**: `/root/.openclaw/workspace/memory/projects/openviking-gemini/task.md`
