# docs-crawler

Description: Crawl and index website documentation via Sitemap XML using Jina Reader.
Usage:
  - Run `node crawl.js` to crawl docs.
  - Automatically filters non-English content and saves to `docs_dump`.

Files:
  - crawl.js: The main crawler script.

