1. **经验提炼与记忆回写**
   - 梳理近期未归档的对话与进展。
   - 如果发现了高价值的转化路径（成功经验）或致命错误，**必须调用 `memory_store` 工具**，将其提炼并永久存入 OpenViking 记忆库。
   - 所有无助于提升 ROI 的冗余日常废话，任其自然过期。

2. **自我编程与自动化**
   - 识别近期重复执行了 3 次以上的任务。如果发现，尝试编写 Python/Bash 脚本将其自动化。

3. **系统备份**
   - 检查距离上次备份是否超过 6 小时。如果是，使用 `exec` 调用 git 命令，将工作区重要代码增量备份到 Github（确保大文件和.env等配置文件被 .gitignore 忽略）。

*(注：如果没有紧急异常或需要主人介入的决策，直接回复 HEARTBEAT_OK，保持静默。)*